image: Ubuntu

stack: jdk 11

branches:
  only:
    - main

build: off

install:
  - java -jar ./artifacts/app-mbank.jar &

build_script:
  -  chmod +x gradlew
  - ./gradlew test --info: Stop-Process -Id $App.Idadlew test --info  # запускаем тест, флаг --info позволяет выводить больше информации